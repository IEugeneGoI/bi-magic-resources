const WIDTH = 500;
const HEIGHT = 300;
const BAR_WIDTH = 80;
const BAR_GAP = 60;
const ARROW_OFFSET = 20;

export const svgSizes = {
    width: WIDTH,
    height: HEIGHT,
    barWidth: BAR_WIDTH,
    barGap: BAR_GAP,
    arrowOffset: ARROW_OFFSET,
};
